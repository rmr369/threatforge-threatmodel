---
name: False positive
about: A rule fired on configuration that is actually fine
labels: false-positive
---

**Rule id:**
**Component:**

**Manifest that triggered it** (redact anything sensitive):

```yaml

```

**Why this is safe in context:**

<!-- e.g. an admission controller mutates it, a mesh enforces mTLS, the
     namespace is unreachable. This tells us whether the fix is a better
     predicate, a lower confidence level, or a compensating-control offset. -->
